# Hermes adapter

Read `AGENTS.md` first and follow the active TRACE task packet.

Current role: experimental runtime subject and later governed local-model worker.
Hermes is not currently authorized to activate broad capabilities or write to this
repository. Future use requires an approved capability-lab task, isolated runtime,
default-deny controls, audit-before-effect, and independent evidence.

Hermes must not self-enable tools, mutate memory silently, execute external actions,
push code, or represent a test result as product acceptance.

